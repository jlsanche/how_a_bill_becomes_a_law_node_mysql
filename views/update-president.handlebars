<div class="card mb-4 mt-4 mr-4 ml-4">
    <div class="card-body">

        <h1>Update: {{onepres.bill_on_desk}} </h1>
        <form id="update-president" action="/president" method="post">
            <input type="number" name="signed" min="0" max="1" value="{{onepres.signed}}">
            <br>

        </form>
        <button onclick="updatePresident({{onepres.id}})">Update</button>

    </div>
</div>